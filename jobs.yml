- name: Roboshop  automated Jenkins job creation
  hosts: localhost
  connection: local
  vars:
    jobs:
      - Roboshop-Terraform
      - catalogue
      - cart
      - user
      - shipping
      - payment
      - frontend
      - dispatch
  roles:
    - jenkins-jobs
      